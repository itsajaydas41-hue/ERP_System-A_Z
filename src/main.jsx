import React from "react";
import ReactDOM from "react-dom/client";
import MillDashboard from "./MillDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MillDashboard />
  </React.StrictMode>
);
